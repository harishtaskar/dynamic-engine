# Copy to .env before running the client:
#   cp .env.example .env
#
# Baked in at build time, so set it before `pnpm build` and rebuild if it changes.
# The client falls back to http://localhost:4000/api when this is unset.

VITE_API_URL=http://localhost:4000/api
