import { Router } from "express";

export const dashboardRouter =
  Router();

dashboardRouter.get(
  "/dashboards",
  
);

dashboardRouter.get(
  "/dashboards/:id",
  
);

dashboardRouter.post(
  "/generate-dashboard",
  
);